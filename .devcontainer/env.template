# Environment Variables Configuration
# ------------------------------------
#       REQUIRED FOR BOT OPERATION
# ------------------------------------

# Discord Bot Token (REQUIRED FOR OPERATION)
# ----------------------------
# 1. Go to Discord Developer Portal: https://discord.com/developers/applications
# 2. Create a New Application
# 3. Navigate to "Bot" in the left sidebar
# 4. Click "Add Bot" (if new application)
# 5. Under "TOKEN" section, click "Click to Reveal Token"
# 6. Copy the token and paste below
# WARNING: NEVER SHARE THIS TOKEN OR COMMIT IT TO VERSION CONTROL!
DISCORD_AUTH_TOKEN="MISSING"

# Server Environment Type
# ----------------------
# Set the environment type for this InfiniBot instance.
# Options:
#   - DEV   (Development: enables debug/test commands)
#   - PROD  (Production: disables debug/test commands)
#
# Example: To run in production mode, set to "PROD" (case-insensitive)
ENVIRONMENT_TYPE="DEV"  # Change to "PROD" for production deployments

# Discord Bot List API Tokens (ONLY FOR OFFICIAL INSTANCE)
# ---------------------------
# Only required if running the official InfiniBot instance with bot list integration
# Repository License prohibits the use any integration outside of the official instance
TOPGG_AUTH_TOKEN="NONE"
DISCORDLISTS_AUTH_TOKEN="NONE"

# Security Notice
# ---------------
# - Never share this file or its contents with anyone
# - Never commit actual token values to Git (keep file in gitignore)
# - Regenerate tokens immediately if accidentally exposed
# - Use different tokens for development and production